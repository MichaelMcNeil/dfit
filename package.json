{
  "name": "dfit",
  "version": "0.0.6",
  "description": "training tools for the dimfit app",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "coveralls": "jest --coverage && cat ./tests/coverage/lcov.info | coveralls",
    "generate-docs": "jsdoc -r  . -d docs -c jsdoc.conf README.md"
  },
  "author": "Michael McNeil",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.24.0"
  },
  "devDependencies": {
    "coveralls": "^3.0.9",
    "docdash": "^1.1.1",
    "jest": "^24.9.0",
    "jsdoc": "^3.6.3"
  }
}
